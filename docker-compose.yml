version: '3'

services:
  mysql:
    build: ./docker/mysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=rootpassword
  apache:
    build: ./docker/apache
    volumes:
      - ./public_html/:/var/www/localhost/htdocs
    ports:
      - "8080:80"
  php:
    build: ./docker/php
    volumes:
      - ./public_html/:/var/www/html/
